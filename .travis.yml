language: rust
rust:
  - 1.31.0

before_script:
  - rustup component add rustfmt-preview

script:
  - cargo fmt --all -- --check
  - cargo build
  - cargo test

notifications:
  email:
    on_success: never
    on_failure: never